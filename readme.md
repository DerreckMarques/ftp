Para executar o serviço FTP execute o comando abaixo
#docker-compose up -d

Para ativar uma nova mudança nas configurações execute o comando abaixo
#docker-compose up --build -d

Para parar o serviço utilize o comando abaixo
#docker-compose stop
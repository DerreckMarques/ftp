Para executar o serviço FTP execute o comando:
#docker-compose up -d

Para ativar uma nova mudança nas configurações execute o comando:
#docker-compose up --build -d

Para parar o serviço utilize o comando:
#docker-compose stop